services:
  - type: web
    name: night-out-together
    rootDir: backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
    envVars:
      - key: ALLOWED_ORIGINS
        value: "*"
    disk:
      name: nightout-data
      mountPath: /data
      sizeGB: 1
